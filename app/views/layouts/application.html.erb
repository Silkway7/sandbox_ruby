<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= full_title(yield(:page_title)) %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_pack_tag 'application' %>
</head>

<body>

<!-- Вывод главного меню -->
<%= yield :main_menu %>

<%# После обращения к нужной странице через контроллер формируется view(отображение) и оно вставляется в body посредством конструкции yield %>
<main class="container mt-3">

  <!-- *************************************************************************-->
  <!---------------------------------->
  <%# flash.each do |k, v| %>
    <!-- <div class="alert alert-<%# k %>">
      <%# v %>
    </div> -->
  <%# end %>
  <!---------------------------------->

  <!-- tag - хелпер, позволяющий сделать код выше короче -->
  <% flash.each do |k, v| %>
    <%= tag.div v, class: "alert alert-#{k}", role: 'alert' %>
  <% end %>
  <!--**************************************************************************-->
  <%= yield %>
</main>

</body>
</html>
