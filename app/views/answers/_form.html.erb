<!-- Подключаем паршел errors, в котором локальная переменная object получит
значение @answer из контроллера страницы-->
<%= render 'shared/errors', object: @answer %>

<!-- Форма создания ответа на вопрос.
в model необходимо добавить массив из моделей Question и Answer, так как путь до ответа
формируется как id вопроса>>>таблица ответов -->
<%= form_with model: [@question, @answer] do |f| %>
  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :body %>
    </div>

    <div class="col-sm-10">
      <%= f.text_area :body, class: 'form-control' %>
    </div>
  </div>
  <%= f.submit "Submit answer!", class: 'btn btn-primary' %>
<% end %>